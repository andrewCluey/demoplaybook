---
  # Play1 - stuff
  - name: Stuff
    hosts: tag_cowsays_true
    become: yes
    become_user: root
    tasks:
      - name: Install cowsays
        apt:
          name: cowsays
          state: present
